---
title: Установка Windows
---

# 💿 Установка Windows

Правильная установка операционной системы — первый шаг к созданию стабильной, безопасной и производительной рабочей среды.

Этот раздел охватывает ключевые аспекты установки Windows на профессиональном уровне.

---

## ⚖️ Выбор редакции

Выбор редакции Windows зависит от ваших задач. Ниже представлена сравнительная таблица ключевых функций.

| Редакция | BitLocker | Hyper-V | RDP-хост | Групповые политики | ReFS | AppLocker / DirectAccess | Целевая аудитория |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| **Home** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | Домашние пользователи. |
| **Pro** | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | Профессионалы и разработчики. |
| **Pro for Workstations** | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | Рабочие станции с мощным "железом". |
| **Enterprise / Education** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | Крупные организации и учебные заведения. |

!!! success "Рекомендация"

    Для большинства технических специалистов, разработчиков и энтузиастов **Windows 11 Pro** является оптимальным выбором, так как предлагает необходимый набор инструментов для профессиональной работы.

### 🆚 Сравнение версий

Помимо редакций, важно понимать различия между основными версиями Windows.

| Версия | Основное предназначение | Интерфейс | Поддержка оборудования | Ключевые особенности |
| :--- | :--- | :--- | :--- | :--- |
| **Windows 10** | **Рабочая лошадка.** Зрелая, стабильная и совместимая с огромным парком старого оборудования. | Классическое меню "Пуск", максимальная кастомизация. | Не требует `TPM 2.0` и `Secure Boot`. | **LTSC-версии** для максимальной стабильности. Огромная база драйверов. |
| **Windows 11** | **Современный десктоп.** Новый интерфейс, улучшенная безопасность и фокус на новом оборудовании. | Центральное меню "Пуск", виджеты, обновлённый дизайн. | **Требует `TPM 2.0` и `Secure Boot`**. Оптимизирована под новые процессоры (например, с P- и E-ядрами). | **WSA (Windows Subsystem for Android)**. Улучшенный планировщик задач. **Auto HDR**. |
| **Windows Server** | **Серверные нагрузки.** Запуск сервисов, управление сетью, виртуализация. | Минималистичный (**Desktop Experience**) или отсутствует (**Server Core**). | Требования зависят от версии, но фокус на серверном "железе". | **Роли и службы** (**Active Directory**, **DNS**, **Hyper-V**). Высокая стабильность и долгосрочная поддержка (**LTSC**). |

### 🗄️ Windows Server

**Windows Server** — это отдельная линейка операционных систем, предназначенная не для повседневной работы или игр, а для запуска сервисов: веб-серверов, баз данных, контроллеров домена и т.д.

| Редакция | Основное отличие |
| :--- | :--- |
| **Standard** | Полный функционал, но ограничено право на запуск 2-х виртуальных машин с Windows Server. |
| **Datacenter** | Предназначена для высоковиртуализированных сред. Позволяет запускать **неограниченное** количество виртуальных машин. |

!!! success "Лучший способ для изучения"

    Если ваша цель — изучить администрирование **Windows Server**, **не устанавливайте её на свой основной ПК**.
    Вместо этого установите её на виртуальную машину с помощью **Hyper-V** (встроен в Windows Pro), **VirtualBox** или **VMware**.

    Это позволит вам безопасно экспериментировать с серверными ролями, не затрагивая вашу основную рабочую среду.

!!! note "Server Core vs Desktop Experience"

    При установке **Windows Server** вам будет предложен выбор:

    *   **Server Core:** Минимальная установка без GUI, управляемая только через командную строку (`cmd`) и `PowerShell`. Рекомендуется для опытных администраторов для повышения безопасности и производительности.
    *   **Desktop Experience:** Установка с привычным графическим интерфейсом (рабочий стол, меню "Пуск").

---

## 💿 UEFI vs Legacy BIOS

Перед установкой Windows важно понимать, в каком режиме будет работать ваша система, так как это влияет на разметку диска и безопасность.

*   **Legacy BIOS + MBR:** Старый стандарт. **MBR (Master Boot Record)** имеет ограничения: поддерживает диски до 2 ТБ и до 4-х основных разделов.
*   **UEFI + GPT:** Современный стандарт. **GPT (GUID Partition Table)** снимает ограничения MBR и является обязательным для некоторых функций.

!!! success "Рекомендация"

    Всегда выбирайте режим **UEFI с разметкой диска GPT**.

    Это стандарт для современного оборудования, который обеспечивает поддержку `Secure Boot` (защита от руткитов на этапе загрузки) и более быструю загрузку системы.

!!! note "Dual Boot"

    При установке Windows рядом с Linux обе операционные системы должны быть установлены в одном режиме (предпочтительно UEFI).

    Установка одной ОС в UEFI, а другой в Legacy приведёт к сложностям с переключением между ними.

---

## 📥 Загрузка ISO-образов

Помимо официального **Media Creation Tool**, для получения `.iso` файлов напрямую с серверов Microsoft можно использовать продвинутые инструменты, которые дают больше контроля.

!!! success "UUP Dump: полный контроль над сборкой"

    **[uupdump.net](https://uupdump.net/)** — это сервис и набор скриптов, которые позволяют скачивать файлы **Unified Update Platform (UUP)** напрямую с серверов Microsoft и собирать из них кастомный `.iso` образ.

    Это основной способ для энтузиастов и DevOps-специалистов для получения самых свежих `Insider` сборок или специфических версий системы.

**Как это работает:**

1.  **Выберите сборку:** На сайте **uupdump.net** перейдите в раздел нужной вам версии (например, `Windows 11 Insider Preview`).
2.  **Выберите билд:** Кликните на конкретный билд (например, `22621.525`).
3.  **Настройте образ:** Выберите язык, редакцию (или несколько) и тип загрузки.
4.  **Скачайте скрипт:** Нажмите "Create download package". Сервис создаст и скачает для вас `.zip` архив со скриптами.
5.  **Запустите сборку:**
    *   Распакуйте архив.
    *   Запустите скрипт для вашей ОС:
        *   Для Windows: `uup_download_windows.cmd`.
        *   Для macOS/Linux: `uup_download_linux.sh` (может потребоваться `chmod +x`).
    *   Скрипт автоматически скачает все необходимые файлы с серверов Microsoft и соберёт из них готовый `.iso` образ в той же папке.

!!! tip "Рекомендации по выбору"

    *   **Какую сборку выбрать?**
        *   **Latest Public Release Build:** Максимальная стабильность. Ваш основной выбор.
        *   **Latest Release Preview Build:** Почти стабильный релиз, позволяет заранее увидеть ближайшие обновления.
        *   **Beta Channel:** Сборки с новыми функциями, которые уже прошли базовую проверку в Dev Channel. Более стабилен, чем Dev, и часто привязан к конкретному будущему релизу.
        *   **Dev Channel:** Оптимальный выбор для тестирования новых функций, которые с высокой вероятностью появятся в будущих релизах. Относительно стабилен.
        *   **Canary Channel:** Для энтузиастов и разработчиков, которым нужен самый ранний доступ к платформенным изменениям. Может быть нестабильным.

    *   **Какие опции выбрать при создании?**
        *   **Download method:** `Download and convert to ISO`.
        *   **Conversion options:** `Integrate updates`, `Run component cleanup`, `Integrate .NET Framework 3.5`.

!!! info "Это безопасно?"

    Да. Как и другие подобные сервисы, **uupdump.net** **не хранит файлы у себя**.

    Он лишь предоставляет скрипты, которые скачивают официальные, нетронутые компоненты напрямую из **Windows Update** и собирают их в образ по инструкциям от Microsoft.

### 📊 Сравнение каналов

В программе **Windows Insider** существует несколько каналов для получения ранних сборок:

| Канал | Стабильность | Частота обновлений | Цель |
| :--- | :--- | :--- | :--- |
| **Beta Channel** | Высокая | ~Раз в месяц | Предрелизное тестирование. Сборки содержат функции, которые почти готовы к публичному релизу. Рекомендуется для раннего знакомства с грядущими обновлениями. |
| **Dev Channel** | Средняя | Раз в 1-2 недели | Тестирование новых функций, которые **планируются** к включению в будущие релизы. Относительно безопасен для использования на вторичных устройствах. |
| **Canary Channel** | Низкая | Несколько раз в неделю | Самый ранний доступ к **платформенным изменениям** ядра и API. Сборки могут быть нестабильными и содержать ошибки. Только для разработчиков и энтузиастов. |

---

## 💾 Загрузочный USB

Хотя утилиты вроде **Rufus** и **Media Creation Tool** удобны, в профессиональной среде часто используются консольные инструменты для автоматизации и полного контроля над процессом.

### 🪟 Для Windows

Этот метод позволяет создать носитель без сторонних утилит.

1.  **Подготовка диска с помощью `diskpart`:** Запустите `cmd` или `PowerShell` от имени администратора:

    ```powershell
    diskpart
    list disk
    select disk X  # Где X - номер вашего USB-накопителя
    clean
    create partition primary
    format fs=fat32 quick
    assign
    exit
    ```

2.  **Копирование файлов с образа:** Смонтируйте `.iso` образ Windows (двойным кликом) и скопируйте все файлы на флешку с помощью `robocopy`.

    ```powershell
    robocopy E:\ F:\ /s /e  # Где E: - смонтированный ISO, а F: - ваша флешка
    ```

### 📦 Интеграция драйверов

Для развёртывания Windows на специфическом оборудовании (например, серверы с RAID-контроллерами) может потребоваться добавить драйверы прямо в установочный образ.

Это делается с помощью `DISM`.

1.  **Смонтируйте образ `install.wim`:**

    Найдите файл `install.wim` (или `install.esd`) в папке `sources` на вашем установочном носителе.

    ```powershell
    # Создать папку для монтирования
    mkdir C:\mount
    # Смонтировать образ (укажите правильный индекс, обычно 1 для Windows Pro)
    dism /mount-wim /wimfile:D:\sources\install.wim /index:1 /mountdir:C:\mount
    ```

2.  **Добавьте драйверы:**

    ```powershell
    # /Recurse позволяет добавить все драйверы из папки
    dism /image:C:\mount /add-driver /driver:C:\drivers /recurse
    ```

3.  **Отмонтируйте образ с сохранением изменений:**

    ```powershell
    dism /unmount-wim /mountdir:C:\mount /commit
    ```

### 🐧 Для macOS и Linux

На Unix-подобных системах основной инструмент — команда `dd`.

!!! danger "Осторожно с `dd`"

    Команда `dd` выполняет побитовое копирование и при неверном указании диска (`of=...`) **безвозвратно уничтожит данные**.

    Дважды проверьте имя диска перед выполнением!

*   **macOS:**

    ```bash
    # Найти диск (например, /dev/disk4)
    diskutil list
    # Отмонтировать его
    diskutil unmountDisk /dev/disk4
    # Скопировать образ (используйте rdisk для ускорения)
    sudo dd if=/path/to/windows.iso of=/dev/rdisk4 bs=1m
    ```

*   **Linux:**

    ```bash
    # Найти диск (например, /dev/sdc)
    lsblk
    # Скопировать образ
    sudo dd if=/path/to/windows.iso of=/dev/sdc bs=1m status=progress
    ```

---

## ⚙️ Ограничения Windows 11

Для установки Windows 11 на неподдерживаемое оборудование можно использовать следующий метод прямо во время установки, не прибегая к сторонним утилитам.

!!! success "Официальный способ через реестр"

    1. На экране выбора диска для установки нажмите `Shift+F10`, чтобы открыть командную строку.
    2. Введите `regedit` и нажмите Enter.
    3. Перейдите в `HKEY_LOCAL_MACHINE\SYSTEM\Setup`.
    4. Создайте новый ключ (Key) с именем `LabConfig`.
    5. Внутри `LabConfig` создайте три параметра `DWORD (32-bit)` со значением `1`:
        * `BypassTPMCheck`.
        * `BypassSecureBootCheck`.
        * `BypassRAMCheck`.
    6. Закройте редактор реестра и командную строку. Проверка будет пропущена.

---

## 🔑 Активация

!!! abstract "Прозрачный метод активации"

    Для активации рекомендуется использовать скрипты с открытым исходным кодом, такие как **Microsoft Activation Scripts (MAS)**.

    Это более безопасная альтернатива закрытым активаторам из неизвестных источников.

!!! example "Активация через MAS (PowerShell)"

    Запустите `PowerShell` от имени администратора и выполните:
    ```powershell
    irm https://massgrave.dev/get | iex
    ```

!!! info "Подробнее про **MAS** см. в (источнике) 👉 [**GitHub репозиторий**](https://github.com/massgravel/Microsoft-Activation-Scripts)."

---

## 🎨 Кастомные сборки

Кастомные сборки — это модифицированные образы Windows, нацеленные на определённую аудиторию.

!!! warning "Используйте на свой страх и риск"

    Автор сборки может внести в систему любые изменения, включая вредоносные.

    Используйте только проверенные проекты с хорошей репутацией.

| Сборка | Целевая аудитория и особенности |
| :--- | :--- |
| **ReviOS** | **Баланс между производительностью и функциональностью.** Отключена телеметрия, но сохранена совместимость с большинством приложений. Хороший выбор для опытных пользователей и геймеров. |
| **AtlasOS** | **Максимальная производительность для игр.** Агрессивно вырезаны многие компоненты системы, что может привести к несовместимости с некоторым ПО. Для тех, кому нужен "чистый" игровой ПК. |
| **Ghost Spectre** | **Гибкость и настройка.** Предлагает несколько версий (Compact, Superlite) и часто включает предустановленные инструменты для твикинга. Для энтузиастов, которые любят настраивать систему под себя. |

---

## 🚀 Развертывание

В корпоративной среде ручная установка Windows на каждый компьютер неэффективна и ведёт к несогласованности конфигураций.

Для решения этих задач используются инструменты автоматизации и масштабирования.

### 🖼️ Sysprep

**Sysprep** (`System Preparation Tool`) — это встроенная утилита Windows, предназначенная для подготовки эталонного образа системы ("золотого образа") к клонированию и развёртыванию на множестве компьютеров.

**Ключевые задачи `Sysprep`:**

*   **Удаление уникальных идентификаторов (SID):** Каждый компьютер в сети должен иметь уникальный SID. **Sysprep** обобщает образ, удаляя SID и другую уникальную информацию.
*   **Подготовка к первому запуску (OOBE):** Настраивает систему так, чтобы при первом запуске на новом оборудовании запустился экран приветствия Windows (**Out-of-Box Experience**), где можно задать имя компьютера, пользователя и т.д.

!!! success "Процесс создания 'золотого образа'"

    1. Устанавливается и настраивается эталонная система (устанавливается ПО, применяются настройки).
    2. Запускается `C:\Windows\System32\Sysprep\sysprep.exe` с опцией `Generalize`.
    3. Система выключается, после чего с диска снимается образ с помощью `DISM` или другого инструмента.

### ⚙️ Unattended установка

Для полной автоматизации процесса установки используется файл ответов `autounattend.xml`.

*   **Что это:** XML-файл, который содержит ответы на все вопросы инсталлятора Windows: от выбора языка и ключа продукта до разбивки диска и создания учётных записей.
*   **Как это работает:** Если файл `autounattend.xml` разместить в корне установочной USB-флешки, программа установки Windows автоматически подхватит его и выполнит все шаги без участия пользователя.

!!! tip "Генерация файла ответов"

    Для создания `autounattend.xml` используется **Windows System Image Manager (WSIM)**, который является частью **Windows Assessment and Deployment Kit (ADK)**.

### 🔑 Лицензирование

В больших организациях используются специальные типы лицензий для массовой активации.

*   **MAK (Multiple Activation Key):** Один ключ, который можно использовать для активации определённого, заранее оговоренного количества компьютеров. Активация происходит один раз через серверы Microsoft.
*   **KMS (Key Management Service):** Стандарт для крупных сетей. Внутри локальной сети разворачивается специальный сервер (KMS-хост), который активирует клиентские машины. Компьютеры должны периодически (обычно раз в 180 дней) обращаться к KMS-серверу для повторной активации