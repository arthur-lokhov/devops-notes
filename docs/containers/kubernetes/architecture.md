---
title: Архитектура
---

# 🏛️ Архитектура Kubernetes: Под Капотом Control Plane

!!! abstract "Философия Kubernetes: Модель Желаемого Состояния"

    Kubernetes — это, по своей сути, **система управления желаемым состоянием**. Ваша задача как инженера — *декларативно* описать, как должна выглядеть ваша инфраструктура (например, «мне нужно 3 реплики веб-приложения»). Kubernetes берёт это описание (**Desired State**), сравнивает его с текущим положением дел (**Actual State**) и постоянно работает над тем, чтобы их синхронизировать.

    Вся архитектура, которую мы изучим, служит одной цели: надёжному и автоматическому **согласованию** (reconciliation) этих двух состояний.

## 🏗️ Общая Модель: Control Plane vs. Worker Nodes

Кластер Kubernetes состоит из двух основных, чётко разделённых логических плоскостей.

### 🧠 Control Plane (Плоскость Управления)

Это **мозг** кластера. Он отвечает за принятие глобальных решений (например, «где запустить этот `Pod`?») и хранение всех метаданных кластера.

*   **Функции:** Хранение состояния, планирование, обнаружение отказов, реагирование на события.
*   **Компоненты:** Набор мастер-процессов, управляющих кластером.

### 💪 Worker Nodes (Рабочие Узлы)

Это **мышцы** кластера. Они выполняют фактическую работу, запуская контейнеры в `Pod`'ах.

*   **Функции:** Запуск приложений, сетевой прокси, мониторинг здоровья локальных ресурсов.
*   **Компоненты:** Локальные агенты, взаимодействующие с `Control Plane` и запускающие контейнеры.

---

## 🧩 Компоненты Control Plane (Мозг Кластера)

Компоненты `Control Plane` обычно работают на выделенных мастер-узлах и являются критически важными для работы всего кластера.

### 💾 etcd: Источник Правды (Source of Truth)

**Что это:** Распределённое, согласованное (highly-consistent) хранилище типа «ключ-значение», основанное на алгоритме **Raft**.

**Назначение:** **Единственный источник правды** для всего кластера. Здесь хранится *абсолютно всё* состояние кластера: манифесты `Pod`'ов, данные `ConfigMap`'ов, текущее количество реплик `Deployment`'ов, сетевые настройки и прочее.

!!! danger "Критические риски etcd"

    1.  **Производительность:** `etcd` очень чувствителен к сетевым задержкам. Задержка между узлами `etcd` выше 50 мс может вызвать нестабильность (split-brain).
    2.  **Бэкапы:** Поскольку это «источник правды», регулярное и надёжное резервное копирование `etcd` — **наиболее критичная SRE-задача** для любого кластера. Потеря данных `etcd` равносильна потере всего кластера.
    3.  **Запись:** Запись в `etcd` блокирующая, поэтому высокая QPS (запросы в секунду) может привести к замедлению всего `Control Plane`.

### 📡 kube-apiserver: Ворота Кластера (The Gateway)

**Что это:** Фронтенд `Control Plane`, единственный компонент, который напрямую взаимодействует с `etcd`.

**Назначение:**
*   Предоставляет **REST API** (точка входа для `kubectl`, контроллеров и `kubelet`).
*   **Аутентификация/Авторизация** (`RBAC`).
*   **Валидация** объектов (принимает только корректные манифесты).
*   **Обнаружение изменений:** Обеспечивает механизм **watch** для всех остальных компонентов.

!!! tip "Ключевая роль Watch-механизма"
    Механизм **Watch** обеспечивает, что контроллеры и `kubelet` не должны постоянно "опрашивать" (`polling`) `API Server`. Вместо этого они поддерживают долгоживущее соединение и **получают уведомления** о новых или измененных объектах. Это критически важно для производительности и низкой задержки.

!!! failure "Что ломается при отказе kube-apiserver"

    Весь кластер становится **Read-Only**. Новые `Pod`'ы не могут быть запланированы или запущены, а существующие `Pod`'ы продолжают работать (пока не требуется вмешательство).

### ⚖️ kube-scheduler: Планировщик Заданий (The Allocator)

**Что это:** Процесс, который следит за новыми `Pod`'ами (через `API Server`) без назначенного узла (`.spec.nodeName` пуст).

**Назначение:** Выбирает **лучший** `Worker Node` для запуска `Pod`'а, используя сложную логику:
*   **Фильтрация (Predicates):** Отсеивает неподходящие узлы (недостаточно ресурсов, не соответствуют `nodeSelector`/`taints`).
*   **Ранжирование (Priorities):** Назначает скоринг подходящим узлам (например, пытается упаковать `Pod`'ы максимально плотно или, наоборот, разнести их).

!!! failure "Что ломается при отказе kube-scheduler"

    Новые `Pod`'ы зависают в состоянии **Pending**. Существующие приложения работают нормально.

### 🤖 kube-controller-manager (The Orchestrator)

**Что это:** Процесс, который управляет множеством «контроллеров» (`Deployment Controller`, `Replication Controller`, `Node Controller` и т.д.).

**Назначение:** Реализует **Reconciliation Loop (цикл согласования)**. Каждый контроллер следит за определённым типом ресурсов и работает над тем, чтобы текущее состояние соответствовало желаемому.

!!! info "Leader Election для высокой доступности (HA)"
    Поскольку в кластере может быть несколько реплик `kube-controller-manager` (для HA), только один из них может быть **Лидером** и активно выполнять цикл согласования. Этот механизм предотвращает **гонки данных** (`race conditions`) и обеспечивается обычно через создание **Lease**-объекта в `etcd` (через `API Server`).

!!! example "Пример работы контроллера"

    **Deployment Controller** следит за `Deployment`'ом. Если желаемое количество реплик равно 3, а текущее — 2, он создаёт 1 новый `ReplicaSet`/`Pod`, чтобы достичь желаемого состояния.

### ☁️ cloud-controller-manager (The Cloud Integrator)

**Что это:** Опциональный компонент, который отделяет логику взаимодействия с облачными провайдерами (AWS, GCP, Azure) от основного кода Kubernetes.

**Назначение:** Управление облачными ресурсами, такими как:
*   **Load Balancers:** Создание облачных балансировщиков для `Service` типа `LoadBalancer`.
*   **Persistent Volumes:** Управление жизненным циклом облачных дисков (`EBS`, `PD`).
*   **Узлы:** Обновление статуса узлов в облаке.

!!! note "Современный подход к облачной интеграции"

    В современных кластерах (особенно в EKS, AKS, GKE) функционал `PV`/`LoadBalancer` часто выносится в отдельные **Controller/Operator'ы** (например, `AWS Load Balancer Controller`) вне `CCM` для большей гибкости.

---

## 💪 Компоненты Worker Node (Мышцы Кластера)

Рабочие узлы содержат всё необходимое для запуска и обслуживания `Pod`'ов, назначенных планировщиком.

### 🧑‍✈️ kubelet: Агент Узла (The Node Agent)

**Что это:** Главный агент, работающий на каждом `Worker Node`.

**Назначение:**
*   **Связь с Control Plane:** Постоянно «смотрит» (watches) на `API Server`, ожидая, что ему будут назначены новые `Pod`'ы.
*   **Управление Pod'ами:** Как только `Pod` назначен, он запрашивает у `CRI`-рантайма запуск контейнеров.
*   **Мониторинг:** Репортит состояние узла (здоровье, ресурсы) и `Pod`'ов обратно в `API Server`.

!!! failure "Что ломается при отказе kubelet"

    Узел помечается как `NotReady` (через несколько минут). `Pod`'ы на этом узле перестают обновляться и не могут быть перенесены до тех пор, пока отказ не будет обнаружен контроллерами.

### 🔗 kube-proxy: Сетевой Прокси (The Network Handler)

**Что это:** Простой сетевой прокси, работающий на каждом узле.

**Назначение:** Поддерживает реализацию абстракции **Service** в Kubernetes. Он следит за `API Server`, обнаруживает новые `Service`'ы и `EndpointSlice` и программирует локальные сетевые правила (обычно **iptables** или **IPVS**), чтобы обеспечить маршрутизацию трафика к `Pod`'ам.

!!! tip "Режимы работы kube-proxy"

    *   **`iptables`**: Традиционный режим, но может медленно масштабироваться при тысячах правил.
    *   **`IPVS`** (IP Virtual Server): Более производителен для большого числа сервисов.

### 📦 CRI, CNI, CSI: Стандартные Интерфейсы

Kubernetes использует стандартизированные интерфейсы, чтобы не зависеть от конкретных реализаций:

1.  **CRI (Container Runtime Interface):** Интерфейс для управления контейнерами.
    *   **Реализации:** `containerd` (наиболее популярный), `CRI-O` (легковесный, только для Kubernetes), `docker-shim` (устаревшее решение для Docker).
2.  **CNI (Container Network Interface):** Интерфейс для настройки сети `Pod`'ов.
    *   **Примеры плагинов:** `Flannel`, `Calico`, `Cilium`. Каждый `Pod` получает IP-адрес через плагин `CNI`.
3.  **CSI (Container Storage Interface):** Интерфейс для управления хранилищем (`PV`/`PVC`).

---

## 🌊 Поток Данных: От `kubectl apply` до Запуска Pod'а

Это ключевой SRE-сценарий, демонстрирующий взаимодействие компонентов:

1.  **Клиент:** Вы запускаете `kubectl apply -f deployment.yaml`.
2.  **API Server:** Получает запрос, аутентифицирует, авторизует и валидирует манифест. Сохраняет новый `Deployment`-объект в **etcd**.
3.  **Controller Manager:**
    *   **Deployment Controller** видит новый `Deployment` в `etcd` (через watch).
    *   Он создаёт соответствующий **ReplicaSet** (RS) и записывает его в `etcd`.
    *   **ReplicaSet Controller** видит новый `RS` и создаёт 3 **Pod**-объекта (записывает их в `etcd`).
4.  **Scheduler:** Видит 3 новых `Pod`'а без назначенного узла (`nodeName` пуст).
    *   Выбирает оптимальный узел (Node A) для первого `Pod`'а.
    *   Обновляет `Pod`-объект в `etcd`, проставляя `.spec.nodeName = "Node-A"`.
5.  **Kubelet:** `Kubelet` на **Node-A** видит (через watch), что `Pod` назначен ему.
    *   Обращается к `CRI`-рантайму (например, **containerd**) с требованием создать `Pod` и его контейнеры.
    *   Обращается к `CNI`-плагину, чтобы выделить `Pod`'у IP-адрес и настроить сетевые правила.
    *   После успешного запуска `Kubelet` обновляет статус `Pod`'а (`Running`) в `API Server` (а значит, и в `etcd`).

---

## ⚙️ Продвинутые Механизмы Управления

### 🔄 Цикл Согласования (Reconciliation Loop)

Это **сердце** Kubernetes. Это не просто одноразовый процесс, а постоянный цикл:

> Контроллер → Смотрит на **Desired State** (`etcd`) → Смотрит на **Actual State** (`kubelet`, cloud) → Если они не совпадают → Выполняет действия для синхронизации → Цикл повторяется

!!! success "SRE-примечание: Устойчивость контроллеров"

    Контроллеры **устойчивы** к отказам. Если контроллер падает, другой его заменит и продолжит работу, потому что всё необходимое состояние (`Desired State`) надёжно хранится в `etcd`.

### 🗼 HA-Модели etcd (Высокая Доступность)

Для обеспечения надёжности `Control Plane` используется несколько узлов (обычно 3 или 5):

*   **Stacked etcd (Наложенный):** `etcd` работает *на тех же* узлах, что и остальные компоненты `Control Plane` (`kube-apiserver`, `scheduler`, etc.).
    *   **Плюсы:** Легче в настройке (например, `kubeadm`), меньше узлов.
    *   **Минусы:** Более высокое потребление ресурсов. Отказ одного узла может повлиять на производительность `etcd`.
*   **External etcd (Внешний/Раздельный):** `etcd` работает на *отдельных*, выделенных узлах.
    *   **Плюсы:** Чёткое разделение ресурсов. Изоляция `etcd` от других нагрузок.
    *   **Минусы:** Увеличение сложности администрирования (больше узлов, отдельный процесс бэкапа/мониторинга).

### 🚦 API Server Priority and Fairness (APF)

Это механизм, критически важный для **стабильности Control Plane под высокой нагрузкой**.

!!! question "Проблема: Перегрузка API Server"

    При интенсивной нагрузке (например, одновременное массовое обновление 1000 `Pod`'ов или запуск большого CI/CD пайплайна) `API Server` может быть перегружен, что приведёт к тайм-аутам для критически важных процессов (`kubelet`, контроллеры) и, как следствие, к нестабильности всего кластера.

**Решение APF:**
1.  **Приоритизация:** Запросы разделяются на **Потоки** (Flows) и **Приоритетные Уровни** (Priority Levels). Критически важные запросы (например, от **system/master** или **kubelet**) получают высший приоритет.
2.  **Очереди (Queuing):** Запросы с более низким приоритетом ставятся в очередь, а не просто отбрасываются.
3.  **Справедливость (Fairness):** Запросы внутри одного Потока (например, от одного пользователя или одного контроллера) обрабатываются «справедливо», чтобы один процесс не монополизировал `API Server`.

!!! abstract "SRE-вывод: Гарантия стабильности"

    `APF` гарантирует, что даже под сильным стрессом `Control Plane` останется **доступным** для своих критических внутренних компонентов.

---

## 🔬 Итоги: Диагностика Отказов Компонентов

Знание того, что ломается при отказе каждого компонента, критично для быстрого траблшутинга:

| Компонент | Признак отказа | Влияние на работающие приложения |
| :--- | :--- | :--- |
| **`etcd`** | Ошибки записи/чтения в `API Server`. | Приложения продолжают работать. Невозможно изменить состояние кластера (создать/удалить/обновить ресурсы). |
| **`kube-apiserver`** | `kubectl` не отвечает. Контроллеры и `Kubelet` не видят изменений. | Приложения продолжают работать. **Весь кластер становится Read-Only.** |
| **`kube-scheduler`** | Новые `Pod`'ы зависают в состоянии `Pending`. | Приложения продолжают работать. Кластер не может масштабироваться. |
| **`kube-controller-manager`** | `ReplicaSet` не создаёт `Pod`'ы. `Endpoint`'ы не обновляются. | Работающие приложения продолжают работать. Автоматическое восстановление (HA) перестаёт работать. |
| **`kubelet`** (на узле) | Узел переходит в состояние `NotReady`. | Приложения на этом узле продолжают работать. Узел изолирован. |
| **`kube-proxy`** (на узле) | Трафик к `Service`'ам на этом узле перестаёт маршрутизироваться. | Приложения на этом узле работают. Сетевой доступ к ним нарушен. |
