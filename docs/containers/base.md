---
title: Основы контейнеризации
---

# 🧱 Основы контейнеризации

**Контейнер** — это стандартная единица программного обеспечения, которая упаковывает код и все его зависимости, чтобы приложение быстро и надёжно работало в любой вычислительной среде.

Это легковесная, изолированная среда выполнения, построенная на принципах разделения ресурсов ядра одной операционной системы.

!!! abstract "Ключевые преимущества"

    *   **Изолированное выполнение:** Запуск разных версий приложений и их зависимостей на одном хосте без конфликтов.
    *   **Консистентность среды:** Гарантия того, что окружение на машине разработчика (**Dev**), в CI/CD-пайплайне и на продуктовом сервере (**Production**) будет идентичным.
    *   **Быстрый старт:** Запуск контейнера занимает миллисекунды, что критически важно для автоматического масштабирования.
    *   **Иммутабельность:** Контейнеры не обновляются и не «патчатся» в рантайме. Вместо этого создаётся новый образ, и контейнеры пересоздаются из него, что соответствует подходу **Infrastructure as Code**.

!!! note "Контейнер vs. Виртуальная Машина (ВМ)"

    Часто контейнеры путают с виртуальными машинами, но они работают на разных уровнях абстракции.

    | Характеристика | Контейнер | Виртуальная Машина |
    | :--- | :--- | :--- |
    | **Изоляция** | Изоляция процессов на уровне ОС | Полная эмуляция "железа" и гостевой ОС |
    | **Ядро ОС** | Общее с хост-системой | Собственное, гостевое ядро |
    | **Размер** | Мегабайты (только приложение и зависимости) | Гигабайты (включая целую ОС) |
    | **Старт** | Миллисекунды | Минуты |
    | **Накладные расходы** | Минимальные | Существенные (CPU, RAM) |

    **Вывод:** Контейнеры намного легче и быстрее, но ВМ предоставляют более строгий, "жёсткий" уровень изоляции.

---

## 📦 Типы контейнеров

Хотя все контейнеры используют одни и те же механизмы ядра, их можно разделить на два типа по философии использования.

| Характеристика | Системный контейнер | Контейнер приложения |
| :--- | :--- | :--- |
| **Назначение** | Запуск полноценной, долгоживущей ОС | Запуск одного процесса/приложения |
| **Содержимое** | `init`, `sshd`, множество служб | Только приложение и его зависимости |
| **Аналогия** | Легковесная Виртуальная Машина | Исполняемый файл (`.exe`, `.jar`) |
| **Основной инструмент** | **Incus** (на базе LXC) | **Docker** |

---

## ⚖️ Что выбрать

Эта таблица поможет определиться с выбором технологии под конкретную задачу.

| Задача | KVM (Виртуальная машина) | Incus/LXC (Системный контейнер) | Docker (Контейнер приложения) |
| :--- | :--- | :--- | :--- |
| **Запуск другой ОС (например, Windows на Linux)** | ✅ **Идеально** | ❌ Невозможно | ❌ Невозможно |
| **Максимальная изоляция и безопасность** | ✅ **Лучший выбор** | 🆗 Хорошо (с `unprivileged`) | 🆗 Хорошо (с `rootless`) |
| **Развёртывание микросервисов** | ❌ Избыточно | 🆗 Возможно, но нетипично | ✅ **Идеально** |
| **Создание легковесных VDS/VPS** | 🆗 Возможно, но ресурсоёмко | ✅ **Идеально** | ❌ Не подходит |
| **Тестовые окружения, имитирующие ОС** | 🆗 Слишком медленно | ✅ **Идеально** | 🆗 Возможно, но сложно |

---

## 🏗️ Экосистема

Фраза «запустить контейнер» скрывает за собой работу нескольких уровней абстракции. Понимание этих уровней — ключ к пониманию всей современной облачной инфраструктуры.

| Уровень | Компоненты | Задача |
| :--- | :--- | :--- |
| **Оркестрация** | **Kubernetes**, **Nomad** | Управление жизненным циклом тысяч контейнеров: масштабирование, отказоустойчивость, сетевое взаимодействие. |
| **Движок (Engine)** | **Docker**, **Podman**, **containerd** | Управление образами, API, сетью, томами. Это основной интерфейс для пользователя. |
| **Среда выполнения (Runtime)** | `runc`, `crun` | Низкоуровневый запуск и изоляция контейнера. Прямая работа с **namespaces** и **cgroups** ядра Linux. |

!!! info "Оркестратор — это не замена Docker"

    Важно понимать, что **оркестратор (Kubernetes)** не заменяет, а **управляет** движками и средами выполнения.

    Типичная связка в production: **Kubernetes** даёт команду **`containerd`** (движок) запустить контейнер, а `containerd` в свою очередь использует **`runc`** (среда выполнения) для непосредственного создания изолированного процесса.

    Docker в этой схеме может отсутствовать, но его компоненты (`containerd`, `runc`) продолжают жить.

!!! abstract "Ключевая мысль"

    **Docker** — это не синоним контейнера. Это всего лишь один из **движков (engine)**, который использует **среду выполнения (runtime)** (например, `runc`) для запуска контейнеров.
    
    Совместимость всех этих инструментов обеспечивается открытыми стандартами [**Open Container Initiative (OCI)**](./oci.md).

---

## 🗄️ Управление данными

Как было сказано ранее, контейнерные образы состоят из слоёв «только для чтения». Все изменения, которые приложение пишет на диск во время работы, попадают во временный, эфемерный слой.

**При остановке и удалении контейнера этот слой уничтожается, и все данные теряются.**

Это идеальное поведение для `stateless`-приложений, но что делать с базами данных или файлами, которые должны пережить перезапуск контейнера?

!!! success "Решение: Volumes (Тома)"

    Для постоянного хранения данных используются **volumes (тома)**.

    Том — это специальная директория, которая «пробрасывается» в контейнер из хост-системы и управляется движком контейнеризации. Данные, записанные в том, сохраняются даже после удаления контейнера.

!!! info "Дальнейшее чтение"

    Более детально работа с томами, их типами и драйверами рассматривается в разделах про [**Docker**](./docker/volumes.md) и [**Incus**](./incus/index.md).

---

## 🔌 Управление сетями

По умолчанию каждый контейнер запускается в своей собственной изолированной сети. Он не «видит» ни хост-систему, ни другие контейнеры, и сам невидим для внешнего мира.

Чтобы разрешить взаимодействие, используются два основных механизма:

1.  **Общие сети:** Контейнеры можно объединить в одну виртуальную сеть, чтобы они могли общаться друг с другом по именам.
2.  **Проброс портов (Port Mapping):** Чтобы сделать сервис, работающий внутри контейнера (например, веб-сервер на порту 80), доступным извне, его порт «пробрасывают» на порт хост-системы. Например, `80 (контейнер) -> 8080 (хост)`.

!!! tip "Ключевой принцип"

    Сетевая изоляция по умолчанию — это важный аспект безопасности. Открывать доступ наружу или к другим контейнерам нужно явно и осознанно.

!!! info "Дальнейшее чтение"

    Детальные настройки сетей, их драйверы и продвинутые сценарии рассматриваются в разделах про [**Docker**](./docker/networks.md) и [**Incus**](./incus/index.md).

---

## 🛡️ Аспекты безопасности

Главный риск безопасности контейнеров и одновременно их главное преимущество — **использование общего ядра с хост-системой**. Это обеспечивает легковесность, но требует многоуровневой защиты (Defense-in-Depth).

!!! danger "Общее ядро — общие риски"

    Уязвимость в ядре хоста потенциально может быть проэксплуатирована из любого контейнера. Прорыв изоляции в этом случае может дать атакующему доступ ко всей хост-системе.

Ниже перечислены четыре фундаментальных принципа для защиты контейнеризированной среды.

**Чем меньше компонентов в образе, тем меньше поверхность для атаки.** Вместо полноценных ОС (вроде `ubuntu`) используйте минималистичные базовые образы:

- **`alpine`**: Легковесный дистрибутив на базе `musl libc`.
- **`busybox`**: "Швейцарский нож" с набором стандартных утилит в одном бинарном файле.
- **`distroless`**: Образы от Google, которые содержат только приложение и его рантайм-зависимости, без оболочки и пакетного менеджера.
- **`scratch`**: Пустой образ, идеален для статически скомпилированных приложений (Go, Rust).

**Запускайте процессы в контейнере от имени обычного пользователя, а не `root`.**

- **`USER` в Dockerfile**: Всегда создавайте непривилегированного пользователя и переключайтесь на него.
- **Rootless-режим**: Запускайте сам движок Docker от имени обычного пользователя. В этом случае, даже если атакующий получит `root` *внутри* контейнера, на хосте у него будут права обычного пользователя.
- **Ограничение `capabilities`**: Запрещайте контейнеру ненужные возможности ядра с помощью флага `--cap-drop=ALL` и добавляйте только те, что действительно необходимы (например, `NET_BIND_SERVICE` для портов < 1024).

**Используйте встроенные в Linux фреймворки безопасности для создания дополнительного «периметра» вокруг контейнера.**

- **`Seccomp`**: Фильтрует системные вызовы, которые может делать процесс. Docker применяет профиль по умолчанию, блокирующий около 44 опасных вызовов.
- **`AppArmor` / `SELinux`**: Ограничивают доступ процесса к файлам, сети и другим ресурсам ядра на основе мандатного контроля доступа (MAC).

**Регулярно проверяйте образы на известные уязвимости (CVE).**

- **Интеграция в CI/CD**: Используйте сканеры вроде **Trivy** или **Grype** для автоматической проверки образов на каждом этапе пайплайна.
- **Анализ зависимостей**: Проверяйте не только пакеты ОС, но и библиотеки вашего приложения (`npm`, `pip`, `maven`).

---

## 🚀 Векторы развития

Экосистема контейнеров не стоит на месте. Ключевые тренды направлены на усиление изоляции, упрощение управления и размытие границ между контейнерами, ВМ и serverless-вычислениями.

!!! abstract "Гибриды ВМ и контейнеров: Безопасность и скорость"

    Проекты вроде **Firecracker** (от Amazon) и **gVisor** (от Google) предлагают лучшее из двух миров: легковесность и быстрый старт контейнеров, но с более строгой изоляцией, близкой к виртуальным машинам.

    В отличие от системных контейнеров **Incus**, которые эмулируют полноценную ОС, **Firecracker** и **gVisor** создают минималистичные, сверхбыстрые "песочницы" с урезанным API, нацеленные на запуск отдельных функций или процессов с максимальной изоляцией.

!!! info "Serverless и Unikernels: Абстракция над контейнерами"

    Контейнеры всё чаще становятся «скрытым» инфраструктурным слоем.

    В **serverless-платформах** (AWS Lambda, Google Cloud Functions) вы просто загружаете код, а облачный провайдер сам упаковывает и запускает его в невидимом для вас контейнере. **Unikernels** идут еще дальше, компилируя приложение и минимально необходимые части ОС в единый, сверхлегкий образ.

!!! success "Rootless как стандарт безопасности"

    Запуск контейнеров и их движков без `root`-прав становится стандартом де-факто для безопасного production-окружения.

    Этот подход кардинально снижает вектор атаки: даже в случае полного "побега" из контейнера, злоумышленник получит права лишь обычного пользователя на хосте, а не `root`.

---

## 🧭 Что дальше?

!!! success "В зависимости от ваших задач, выберите подходящий путь"

    1.  **Контейнеризация приложений (один хост):**
        Для создания образов и запуска контейнеров на одной машине стандартом является **Docker**.
        → [**Перейти к разделу Docker**](./docker/index.md)

    2.  **Системные контейнеры (аналоги ВМ):**
        Для управления «полноценными» системными контейнерами, имитирующими ОС, используется менеджер **Incus**.
        → [**Перейти к разделу Incus**](./incus/index.md)

    3.  **Оркестрация в кластере (production):**
        Для управления контейнерами в production-среде на нескольких хостах используется **Kubernetes**.
        → [**Перейти к разделу Kubernetes**](./kubernetes/index.md)